# 授权他人代表我行事（删除个人信息）

我，**{full_name}**，特此授权该工具的运营方作为我的代理人，仅限执行以下任务：根据相关隐私法规（如CCPA/CPRA、GDPR）代表我向数据中介机构和人肉搜索网站提交删除、屏蔽以及禁止出售/共享个人信息的请求。

本授权仅限于个人信息删除事宜，不涵盖对我其他任何信息的使用。

- 全名：{full_name}
- 日期：{date}
- 签名：____________________________

请将已签名的文件保存在档案中的 `consent.authorization_artifact` 路径下。仅当 `consent.method` 设定为 `written_authorization` 或 `poa` 时才需要此步骤（对于 `self` 模式则无需）。
